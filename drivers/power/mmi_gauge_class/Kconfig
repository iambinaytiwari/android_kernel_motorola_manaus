config MMI_GAUGE_CLASS
	tristate "MMI GAUGE CLASS"
	default n
	help
	  Say Y here if you have a mmi gauge class compatible
	  device connected.

	  This driver can also be built as a module.  If so, the module
	  will be called mmi-gauge-class.
